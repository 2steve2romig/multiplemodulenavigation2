{
  "name": "suretrend-platform",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "echo 'No build step — static site with serverless functions in api/'",
    "test": "jest",
    "test:unit": "jest test/unit",
    "test:integration": "jest test/integration"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
